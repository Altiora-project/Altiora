export const routes = {
  mainPage: '/',
  ui: '/ui',
  services: '/services',
  postList: '/posts',
  post: '/posts/:id',
  complexRout: '/complex-rout/:id/slug/:value',
  company: '/',
  partners: '/',
  contacs: '/'
} as const
